/**
 * Auth Types - Interfaces related to authentication and protected routes
 */

export interface ProtectedRouteProps {
  redirectTo?: string;
}

export interface PublicRouteProps {
  redirectTo?: string;
}
